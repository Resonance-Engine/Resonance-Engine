"""Integration test: EDGAR fetch → parse → normalize → dedup → store."""

# TODO: Phase 0 — requires PostgreSQL + Redis running
# - test_ingest_8k_filing_end_to_end
# - test_duplicate_filing_skipped
# - test_change_gate_filters_boilerplate
