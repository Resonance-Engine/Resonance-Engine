"""NewsAPI client — breaking news aggregation (Phase 2)."""


async def fetch_top_headlines(category: str = "business", country: str = "us") -> list[dict]:
    """Fetch top headlines from NewsAPI.

    TODO: Phase 2 — implement NewsAPI integration (free tier: 100 req/day)
    """
    raise NotImplementedError("NewsAPI client — Phase 2 deliverable")
