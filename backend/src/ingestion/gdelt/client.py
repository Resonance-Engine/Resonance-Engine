"""GDELT API client — global news event detection (Phase 2)."""


async def search_articles(query: str, timespan: str = "15min") -> list[dict]:
    """Search GDELT for recent articles matching a query.

    TODO: Phase 2 — implement GDELT DOC API integration
    """
    raise NotImplementedError("GDELT client — Phase 2 deliverable")
