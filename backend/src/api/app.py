"""FastAPI application factory — REST API for Resonance Engine (Phase 2)."""

# TODO: Phase 2 deliverable
# from fastapi import FastAPI
# from src.api.routes import events, signals, entities, watchlist, health


def create_app():
    """Create and configure the FastAPI application.

    TODO: Phase 2 deliverable
    - Initialize FastAPI with title, version, docs
    - Include route modules
    - Add middleware (CORS, rate limiting, auth)
    - Configure lifespan (DB connection pool)
    """
    raise NotImplementedError("FastAPI app — Phase 2 deliverable")
